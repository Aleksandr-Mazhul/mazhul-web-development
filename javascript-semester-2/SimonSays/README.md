планирование Simon says

1. классы делать и в целом какая структура:
    - класс Cards в котором будет храниться вся информация о текущей последовательности, он должен уметь следующее:
        - хранить текущий порядок карточек:
            - это должен быть массив с идентификаторами, где каждому идентификатору соответствует отдельный цвет
              карточки (на основании этих id надо будет потом генерировать dataId)
        - уметь добавлять карточки в свою последовательность
        - уметь отслеживать и сохранять текущий рекорд
        - сохранять данные в local storage
        - забирать данные из local storage (static метод)

    - класс Player, в котором будет храниться текущая информация о ходе игрока, а именно:
        - массив с текущей последовательностью выбранных карточек 
        - рекорд игрока
        - число очков (количество правильно выбранных карточек)
    - структура State, в которой будет храниться два состояния await playMove
    - класс Game, в котором будет происходить основная логика игры, в нем должны быть следующие методы:
        - init (подготовка к игре)
            - забрать данные из local storage
            - восстановить на их основе класс Cards
            - восстановить на их основе класс Player
            - render
        - сохранять данные в local storage
        - забирать данные из local storage (static метод)
        - playMove (сделать ход по нажатию на кнопку)
            - async function, которая делает отсчет ready / set / go
            - функция, которая отображает текущую карточку
            - функция nextMove
                - добавляет 1 к текущим очкам
                - генерирует новую карточку
            - асинхронная функция которая ждет нажатие по родительскому элементу, и проверяет на какую кнопку нажали,
              если кнопка правильная, то надо перейти к следующему ходу (nextMove), если нет, то надо сбросить текущий
              результат и начат игру заново
            - resetGame. Функция, которая при нажатии на кнопку будет перезапускать игру
      
2. реализация